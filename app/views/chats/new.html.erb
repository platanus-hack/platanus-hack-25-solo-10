<% content_for :title, "New chat" %>

<div class="max-w-2xl mx-auto p-6 bg-white">
  <h1 class="text-2xl font-bold text-[var(--color-deep-space-blue)] mb-6">New chat</h1>

  <%= render "form", chat: @chat %>

  <div class="mt-6">
    <%= link_to "Back to chats", chats_path, class: "text-[var(--color-yale-blue)] hover:opacity-80 underline" %>
  </div>
</div>